<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#dfdfdf"/>

    <!-- Bordes redondeados -->
    <corners android:radius="5dp"
        />

    <!-- Borde exterior -->
    <stroke
        android:width="2dp"
        android:color="#dfdfdf"/> <!-- Un morado, cámbialo al color que quieras -->

    <!-- Margen interior -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
